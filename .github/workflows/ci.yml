name: CI
on:
  push:
    branches: ["**"]
  pull_request:
jobs:
  structure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f README.md && test -f backend/app/main.py && test -f frontend/src/App.vue && test -f node-agent/README.md
